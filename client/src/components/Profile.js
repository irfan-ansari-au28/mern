import React from "react";

const Profile = ({ _id: id, name, email }) => {
  return (
    <>
      <div>{{ id }}</div>
      <div>{{ name }}</div>
      <div>{{ name }}</div>
    </>
  );
};

export default Profile;
